name: Check Flats

on:
  schedule:
    - cron: '0 * * * *'  # Запуск щогодини
  workflow_dispatch:     # Кнопка для ручного запуску

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: write    # Даємо право боту записувати файл бази даних
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0 # Потрібно для успішного збереження файлів

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run monitor
        run: python monitor.py

      - name: Save state
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add seen_ids.txt
          git commit -m "Update seen IDs [skip ci]" || exit 0
          git push origin main --force
